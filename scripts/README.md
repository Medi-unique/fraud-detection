# Scripts

CLI / batch helpers for the fraud-detection pipeline.

Place one-off download, training, or export scripts here. Core logic lives in `src/`; notebooks orchestrate exploration.

Example (future):

```bash
python scripts/train_fraud_models.py --dataset ecommerce
```
